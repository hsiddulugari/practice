// Pring all combinations of well formed parens
// ()
// (()) ()()
// ((())) (()()) (())() ()(()) ()()()

// Find if sum of not necessarily contiguous integers in a given list of integers equals a value
#include <iostream>

using namespace std;

bool findSumExists(vector <int> input, int ik)
{
}

int
main()
{

}
